{
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "praise-app-backend",
    "praise-app-frontend"
  ],
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "pinst": "^3.0.0"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": "eslint --cache --fix --report-unused-disable-directives --max-warnings 0",
    "*.{css,scss,md}": "prettier --write"
  }
}
